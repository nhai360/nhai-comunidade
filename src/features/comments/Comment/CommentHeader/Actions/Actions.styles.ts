import { styled } from "@/../stitches.config";

export const Container = styled("div", {
  display: "flex",
  alignItems: "center",
  gap: "$4",
});

export const Box = styled("div", {
  display: "flex",
  alignItems: "center",
  gap: "$2",
});
