import { styled } from "@/../stitches.config";

export const Container = styled("div", {
  display: "flex",
  alignItems: "center",
});

export const Actions = styled("div", {
  display: "flex",
  alignItems: "center",
  gap: "$2",
  marginLeft: "auto",
});
